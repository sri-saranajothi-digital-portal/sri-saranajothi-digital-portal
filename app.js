const books=window.BOOKS||[],grid=document.getElementById('bookGrid'),searchInput=document.getElementById('searchInput'),categoryFilter=document.getElementById('categoryFilter'),shelfFilter=document.getElementById('shelfFilter'),resultCount=document.getElementById('resultCount'),loadMore=document.getElementById('loadMore'),resetBtn=document.getElementById('resetBtn');let visible=18;const uniq=a=>[...new Set(a.filter(Boolean))].sort((x,y)=>x.localeCompare(y,'si')),categories=uniq(books.map(b=>b.category)),shelves=uniq(books.map(b=>b.shelf));document.getElementById('statBooks').textContent=books.length;document.getElementById('statCats').textContent=categories.length;categories.forEach(v=>categoryFilter.insertAdjacentHTML('beforeend',`<option>${v}</option>`));shelves.forEach(v=>shelfFilter.insertAdjacentHTML('beforeend',`<option>${v}</option>`));function esc(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function filtered(){const q=searchInput.value.trim().toLocaleLowerCase('si');return books.filter(b=>{const hay=`${b.id} ${b.title} ${b.author} ${b.category} ${b.shelf} ${b.ddc}`.toLocaleLowerCase('si');return(!q||hay.includes(q))&&(!categoryFilter.value||b.category===categoryFilter.value)&&(!shelfFilter.value||b.shelf===shelfFilter.value)})}function render(reset=false){if(reset)visible=18;const list=filtered(),shown=list.slice(0,visible);resultCount.textContent=`ප්‍රතිඵල ${list.length}ක් හමු විය`;grid.innerHTML=shown.length?shown.map(b=>`<article class="book-card"><div class="book-top"><span class="book-icon">📘</span><span class="book-id">${esc(b.id)}</span></div><h3>${esc(b.title)}</h3><p class="meta"><strong>කර්තෘ:</strong> ${esc(b.author||'—')}</p><p class="meta"><strong>විෂය:</strong> ${esc(b.category||'—')}</p><p class="meta"><strong>රාක්කය:</strong> ${esc(b.shelf||'—')}</p><div class="badges">${b.ddc?`<span class="badge">DDC ${esc(b.ddc)}</span>`:''}<span class="badge available">✓ පුස්තකාලයේ ඇත</span></div></article>`).join(''):`<div class="empty">ඔබ සෙවූ පොත හමු නොවීය.</div>`;loadMore.style.display=visible<list.length?'block':'none'}[searchInput,categoryFilter,shelfFilter].forEach(el=>el.addEventListener(el.tagName==='INPUT'?'input':'change',()=>render(true)));loadMore.addEventListener('click',()=>{visible+=18;render()});resetBtn.addEventListener('click',()=>{searchInput.value='';categoryFilter.value='';shelfFilter.value='';render(true)});document.getElementById('menuBtn').addEventListener('click',()=>document.getElementById('nav').classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>document.getElementById('nav').classList.remove('open')));render();
